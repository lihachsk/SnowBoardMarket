import {FETCH_SNOWBOARD_BY_ID_SUCCESS} from 'actionTypes';
import * as R from 'ramda';
import { Reducer } from 'redux';
export const actionCreators = {
    fetchSnowboardById: (id: string) => async (dispatch: any) => {},
    addSnowboardToBasket: (id: string) => (dispatch: any) => {}
}

export class ISnowboard {
    snowboard: any;
    id: string;
}

const initialState = {
    id: null,
    snowboard: null
} as ISnowboard

export const reducer: Reducer<ISnowboard> = (state: ISnowboard = initialState, {type, payload}) => {
    switch (type) {
        case FETCH_SNOWBOARD_BY_ID_SUCCESS:
            return R.merge(state, {
                id: R.prop('id', payload)
            })
        default: 
            return state; 
    }
}